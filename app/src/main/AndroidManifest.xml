<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.friendlychat">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".Module.AppStateDetector"
        android:allowBackup="true"
        android:icon="@mipmap/ic_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SignInActivity"></activity>
        <activity
            android:name=".Activities.ContactsActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- android:name="androidx.multidex.MultiDexApplication" -->
        <provider
            android:name=".DB.MessagesProvider"
            android:authorities="com.example.friendlychat"
            android:enabled="true"
            android:exported="false" />

        <activity
            android:name=".Activities.ChatsActivity"
            android:theme="@style/ChatsTheme" />
    </application>

</manifest>